#!/bin/bash
set -e

# cat <(dmenu_path) <(cut -d ';' -f 2- < ~/.zhistory) | fzf --tiebreak=index --print-query --prompt='' | tail -1
dmenu_path | fzf --tiebreak=index --print-query --prompt='' | tail -1 | tee $1
